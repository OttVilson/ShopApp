import { ShortcutNavDirective } from './shortcut-nav.directive';

describe('ShortcutNavDirective', () => {
  it('should create an instance', () => {
    const directive = new ShortcutNavDirective();
    expect(directive).toBeTruthy();
  });
});
